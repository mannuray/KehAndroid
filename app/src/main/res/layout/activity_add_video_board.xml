<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.dubmania.dubsmania.misc.AddVideoBoardActivity">


    <include
        android:id="@+id/tool_bar"
        layout="@layout/tool_bar"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"/>

    <EditText
        android:layout_below="@+id/tool_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center|top"
        android:id="@+id/add_video_board_name_edit" />

    <GridView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:columnWidth="60dp"
        android:numColumns="auto_fit"
        android:id="@+id/add_video_board_gridView"
        android:layout_above="@+id/add_video_board_add_button"
        android:layout_below="@+id/add_video_board_name_edit" />

    <Button
        android:layout_width="200dp"
        android:layout_height="75dp"
        android:text="New Button"
        android:id="@+id/add_video_board_add_button"
        android:onClick="addVideoBroard"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true" />
</RelativeLayout>
